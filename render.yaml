services:
  - type: web
    name: login-intern-backend
    env: node
    plan: free
    buildCommand: "cd server && npm install"
    startCommand: "cd server && node index.js"
    envVars:
      - key: PORT
        value: 5005
      - key: JWT_SECRET
        generateValue: true
